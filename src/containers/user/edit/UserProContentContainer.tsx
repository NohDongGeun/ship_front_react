import React from 'react';
import EditorWithLabel from '../../../components/commons/EditorWithLabel';

const UserProContentContainer: React.FC = () => {
    return <EditorWithLabel />;
};

export default React.memo(UserProContentContainer);
