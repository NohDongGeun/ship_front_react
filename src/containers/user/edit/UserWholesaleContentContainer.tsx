import React from 'react';
import EditorWithLabel from '../../../components/commons/EditorWithLabel';

const UserWholesaleContentContainer: React.FC = () => {
    return <EditorWithLabel />;
};

export default React.memo(UserWholesaleContentContainer);
